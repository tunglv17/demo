import React from 'react'

const LayoutWebsite = (props) => {
    return (
        <div className='container'>
            {props.children}
        </div>
    )
}

export default LayoutWebsite
